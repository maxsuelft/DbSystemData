apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbsystemdata.fullname" . }}-service
  namespace: {{ include "dbsystemdata.namespace" . }}
  labels:
    {{- include "dbsystemdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dbsystemdata.selectorLabels" . | nindent 4 }}
---
{{- if .Values.service.headless.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbsystemdata.fullname" . }}-headless
  namespace: {{ include "dbsystemdata.namespace" . }}
  labels:
    {{- include "dbsystemdata.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "dbsystemdata.selectorLabels" . | nindent 4 }}
{{- end }}
